# 短篇真机测试汇总

测试日期：2026-08-12  
测试方式：Chrome 真机浏览器，`http://localhost:3000/`  
测试配置：默认武侠开局、短篇、普通内容强度；新建游戏后从序幕开始操作。

## 结论

已完成一轮完整短篇流程并到达胜利结局。最终成功路径在第 5 回合收束，包含序幕确认、地图进入青石镇、剧情建筑进入、固定选项、自定义回应和结局生成。测试期间刷新浏览器 3 次，均能从当前存档恢复并继续流程。

## 流程记录

| 阶段 | 浏览器操作 | 结果 |
| --- | --- | --- |
| 开局 | 提交默认开局，点击“开始冒险” | 序幕生成并进入世界地图 |
| 第一、二幕 | 进入青石镇、进入剧情建筑、与 NPC 交谈 | 任务推进，新增剧情 NPC |
| 第三幕 | 选择“表示愿意支持传讯人·3” | 第三幕任务 `循迹而行·第3幕` 成功具象化 |
| 自由回应 | 输入“我愿意协助你，请告诉我最后的真相。”并发送 | API 回合完成，生成结局 |
| 结局 | 读取结局面板 | 显示“胜利”，可点击“重新开始” |

## 测试中修复的问题

### 1. 未具象化最终幕任务时提前进入结局

第一次流程在第 4 回合直接显示结局，当时故事已跳到目标幕，但目标幕主线任务尚未生成，导致规则引擎把已有任务全部完成误判为可结局。

已在 `src/game/gameplay/rpg/ruleEngine/advanceStoryProgression.ts` 修复：只有当前幕主线任务已存在且全部完成时，才允许生成结局；缺少当前幕任务时继续请求下一幕具象化。

### 2. 动态演化复用 NPC / 任务名称，导致下一幕写回失败

修复提前结局后，第三幕演化提案仍使用重复的“传讯人”和“循迹而行”，被世界状态的重复名称约束拒绝，流程停在“需要下一幕”。

已在 `src/game/application/deterministicEvolutionSource.ts` 增加确定性的重名后缀策略，例如 `传讯人·3`、`循迹而行·第3幕`，并补充回归测试。

### 3. API 调用期间缺失阻塞模态

原状只有场景内的“正在编排下一幕……”或底部状态文字，已有的 `.narrative-generation-modal` CSS 没有对应 JSX 渲染，因此用户无法明确知道页面正在等待 API，也可能误以为操作没有生效。

已恢复统一的 `GenerationStatusModal`，接入：

- 新建游戏生成；
- 序幕确认请求；
- 行动提交；
- 下一幕叙事生成。

Chrome 真机已确认：提交行动后出现遮罩、旋转指示和“正在处理……”；生成下一幕后显示“正在编排下一幕……”，期间人物、行动和对话按钮均禁用。

### 4. 剧情建筑自动聚焦旧 NPC

动态新增 `传讯人·3` 后，进入铁匠铺曾自动打开旧的“传讯人”对话，而 HUD 当前目标已是“与传讯人·3交谈”。

已在 `src/components/AdventureGameShell.tsx` 优先按当前目标解析 NPC，再回退到建筑绑定 NPC，避免进入建筑后打开错误对话。

### 5. HUD 目标文案与实际状态不一致

最终幕任务完成、结局仍在生成时，HUD 曾显示“暂无线索”，与场景中的任务完成提示不一致。

已在 `src/components/AdventureHud.tsx` 优先使用 canonical `currentObjectiveLabel`，并在故事已完成但结局尚未落地时显示“结局生成中”。同时将 API 返回的英文 `Action performed` 映射为“行动已完成”。

## 后续 UI 优化建议（本轮未修改）

以下问题不阻塞通关，本轮仅记录：

1. 场景中叙事旁白和地点描述会同时展示相同内容，造成重复阅读；建议区分“当前事件”和“地点常驻描述”，或折叠其中一项。
2. AI 生成文本出现“卷起。。”这类重复标点；建议在展示层增加轻量标点清洗，同时保留原始文本用于日志。
3. “与 NPC 交谈”存在中间态：先显示单个“与某人交谈”按钮，再异步变成两个固定选项和自定义输入；建议明确标注“正在准备对话”，减少用户对按钮数量变化的困惑。
4. 生成耗时可能达到几十秒；当前模态已能说明正在等待，后续仍建议增加阶段进度、耗时提示或失败重试入口。
5. 小镇地图的剧情建筑与普通建筑视觉区分仍不够强，提示“点击高亮建筑”但高亮状态不总是明显；建议增加颜色、描边或“当前剧情”标签。
6. 场景底部行动栏在无行动时只留下空区域，建议显示明确的“等待剧情推进”或“可探索内容将在下一幕出现”。

## 验证结果

- `npm run typecheck`：通过。
- `npm run lint`：通过，无 error；保留项目原有 warning。
- `npm run test:game-application -- src/components/AdventureGameShell.test.tsx src/components/CurrentGameScreen.test.tsx`：36 个文件、374 个测试通过。
- `npm run test:game-gameplay -- src/game/gameplay/rpg/ruleEngine/advanceStoryProgression.test.ts`：35 个文件、319 个测试通过。
- `npm run build`：通过，Next.js 生产构建成功。
- `git diff --check`：通过。
