# 共享候选登记表

> 只有在第二个真实消费者出现时，才按 `共同规范/共享模块开发流程.md` 评估抽取。

| 能力 | 状态 | 当前项目使用 | 另一项目使用 | 结论 |
|---|---|---|---|---|
| `@ai-game/ui` | shared | `NewGameSetupForm` 使用 `Panel`、`InlineButton`、`Tag` | SLG 通过原路径 adapter 被现有业务组件广泛消费 | v0.1 进入 foundation；主题和业务 UI 分别维护 |
| `@ai-game/ai-transport` | candidate | MVP Phase 4 将需要 OpenAI-compatible transport | SLG 已有 `src/game/core/ai/` | 等 RPG 首次真实调用时再抽取 |

状态仅可使用：`local`、`candidate`、`evaluating`、`shared`、`rejected`。
