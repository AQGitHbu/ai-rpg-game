# 共享候选登记表

当前没有登记中的待抽取候选。已建立的 package 以 [共享模块目录](共同规范/共享模块目录.json) 为准，不在本表复制版本与导出。

出现第二个真实消费者时，按 [共享流程](共同规范/共享模块开发流程.md) 记录能力、两个消费者的同语义证据和抽取结论。候选状态使用 `local`、`candidate`、`evaluating` 或 `rejected`；能力进入公共目录后移出本表。
