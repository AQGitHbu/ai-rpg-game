# 地图与地点冒险

## 系统定位

`GameSessionView` 将唯一权威状态投影成世界地图、当前地点、城镇层、在场 NPC、可取得物品、叙事场景、战斗与结局。`AdventureGameShell` 只渲染这些事实并提交 opaque token/free text，不再维护并行旧视图或从文案推导 Action。

## 当前 read model

- `worldMap.locations`：地点名称、current/visited 状态和可选 `travelChoice`。
- `currentLocation`：名称、描述、`scale`（`scene` / `town`）和当前合法 actions。
- `currentLocation.town`（仅 `scale: "town"`）：确定性城镇快照、展示标签与可交互建筑条目（`TownView`）；正常使用 slot 绑定，旧存档若仅保存了当前地点 NPC，则在读模型中补入空闲剧情 slot，不写回存档；不暴露 seed、空闲 slot、生成器内部、隐藏 NPC 或未批准地点。
- `narrative`：ready scene、叙述、NPC 台词、两个批准选择和 `npcDialogues`。
- `NpcDialogueView`：焦点 NPC 有两个固定 choices 且 `freeInputEnabled: true`；非焦点 NPC 不拥有该输入能力。
- `obtainableItems` / `inventory`：只含玩家可见资料与合法 choice。
- `battle`：active battle controls；`ending`：结构化结局。
- `narrativeGeneration.pending`：阻止并发玩家 Action，并触发 ensure/polling。

## 三层导航

- 地图（世界地图旅行层）→ 城镇（`scale: "town"`，`TownLayerScreen`）→ 建筑场景（`LocationSceneScreen`）。
- 从建筑场景返回城镇；从城镇返回世界地图。`scale: "scene"` 地点保持地图 → 场景两层直达。
- 城镇几何与剧情建筑 slot 在开局预分配（`createTownRuntime`，确定性 seed 重建），运行时 NPC 才绑定到 slot（`bindNpcToTownSlot`，取第一个空闲 slot）；进入已绑定建筑时使用当前地点下发的 opaque talk token 发起正式对话回合，生成完成后才打开焦点 NPC 对话。
- 城镇层是纯 UI 导航，不改变位置指针粒度，也不新增 `enter_building` 意图或 `/api/game/town/ensure` 路由。
- 城镇地图按视口高度约束 SVG 尺寸，桌面端与建筑入口面板并列；避免地图撑出固定游戏视口后把“进入建筑”操作推到不可见区域。
- 新局创建时首场景可处于 `narrativeGeneration.pending`；黑屏序幕确认前不启动 ensure 轮询，确认后才生成并刷新首场景，避免序幕与首场景异步刷新重叠。

## 客户端边界

- 客户端只能发送 read model 下发的 `choiceToken` 或焦点 NPC 自定义文本。
- UI 不拼接 `move:*`、`talk:*` 等业务 token，不解析 token，不接收 `actionKey`。
- 每次提交由 `gameActionRequest` 创建 UUID；固定和自定义输入共享 `/api/game/actions`。
- 地图导航中的纯 UI 切换不写状态；真正移动只通过 `travelChoice`。
- pending、stale、网络错误与结局状态必须显示稳定反馈，不能伪造成功。

## 主要文件与测试

- `src/game/application/gameSessionView.ts` — 唯一完整 read model projector。
- `src/game/application/townView.ts` — 城镇层 read model（快照 + 可交互建筑）。
- `src/game/domain/townState.ts` / `src/game/gameplay/rpg/town/` — 确定性城镇生成器与 slot 绑定。
- `src/components/CurrentGameScreen.tsx` — 创建/恢复/ensure/ack/clear 协调器。
- `src/components/AdventureGameShell.tsx` — 地图、城镇、场景、NPC、战斗和结局呈现。
- `src/components/TownLayerScreen.tsx` — 城镇层 UI。
- `src/components/gameActionRequest.ts` — 统一行动请求。
- `src/components/AdventureGameShell.test.tsx` — token 消费、NPC 双选项 + 自定义输入、三层导航。
- `src/game/application/gameSessionView.test.ts` — 零泄漏、当前合法 choice 与焦点 NPC 投影。
- `src/app/api/game/routeContract.test.ts` — 六 route 精确合同。

## 修改注意事项

- 新展示事实先进入 `GameSessionView`，不让组件 deep-import domain/gameplay。
- read model 不能泄露未发现 fact、内部 ID、registry、effect、seed 或 AI diagnostics。
- 不为 UI 便利增加 adapter、compat view 或第二套 session type。
