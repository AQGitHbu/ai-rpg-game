# 探索与任务推进

## 职责

本系统把探索、交谈、移动、拾取、战斗结果和事实发现转换为结构化事件，再由任务 reconciliation 推进 objective、主线幕、可达路线和结局条件。叙事文本与玩家宣称不能直接完成任务。

## 当前契约

- 当前客户端不提供泛化 `explore` 或独立调查按钮；环境观察直接展示。内部 `explore` 可承载已批准的具体目标建筑抵达步骤，只有匹配当前目标与活跃 bundle 时才下发建筑入口 token。
- `investigate` 保留在 Action 与 bundle trigger 中，供显式 offline fixture 或内部规则测试消费；新 read model 不投影调查方式。连续的当前地点 `discover_fact` 目标在同一规则边界有界确认，遇到其他目标或异地事实即停止；城镇目标须先进入承载建筑。
- `talk` 要求 NPC 在场；`move` 要求目标地点已解锁且可从当前位置到达，已访问地点可从地图回访；`take_item` 要求物品属于当前地点且尚未拥有。
- `attack` / `battle_action` 只处理合法敌人与 active battle。任务 objective 支持访问地点、交谈 NPC、取得物品、发现事实和击败敌人。
- `town_building` 是当前城镇内的建筑承载，不铸造世界地图地点；需要离镇旅行时才使用独立 `world` placement。
- 生产 `move`、`explore`、`take_item`、`give_item` 和战斗边界只消费 `narrativeBundle` 的权威步骤；缺步骤返回稳定 continuation 错误并零写入。`PreparedContinuationState` 仅用于 offline fixture。
- 任务链短暂没有 active objective 时，read model 可派生在场 NPC 或未到访地点的桥接目标；桥接不写入状态，权威任务铸造后自动让位。不以泛化探索充当下一幕或终幕生成入口。
- 场景拾取与 NPC 给予的规则见[物品与任务奖励](物品与任务奖励.md)。旅行、交付和开战仍需明确玩家选择。

## 必要流程

```text
合法 Action → resolveTurn → 自动揭示 / objective reconciliation / candidate event
→ committed event 与状态变更 → 生产 bundle step 消费
→ GameSessionView 投影下一入口
```

移动抵达场景地点后，规则先推进揭示游标，再自动确认连续事实目标。城镇容器不证明已经到达目标建筑，其事实由具体建筑抵达边界承接。AI 场景内容表达规则结果，不决定事实发现或张力数值。

## 主要源码和验证

- `src/game/gameplay/rpg/ruleEngine/validateAction.ts`、`src/game/gameplay/rpg/ruleEngine/resolveByType.ts`、`src/game/gameplay/rpg/ruleEngine/reconcileQuests.ts`、`src/game/gameplay/rpg/ruleEngine/advanceStoryProgression.ts`
- `src/game/application/buildChoiceMap.ts`、`src/game/application/gameSessionView.ts`、`src/game/application/performTurn.ts`
- `src/game/gameplay/rpg/worldEvolution/index.ts`、`src/game/gameplay/rpg/narrativeBundle/index.ts`
- `src/game/application/testing/foundationJourney.test.ts`、`src/game/application/testing/investigationChoiceJourney.test.ts`、`src/game/application/testing/storyDivergenceJourney.test.ts`、`src/game/application/testing/npcContinuityJourney.test.ts`
- `src/game/gameplay/rpg/ruleEngine/index.test.ts`

## 按条件关联文档

- UI 入口和地图/城镇层级见 [地图与地点冒险](./地图与地点冒险.md) 与 [小镇程序化生成](./小镇程序化生成.md)。
- 物品目标见 [物品与任务奖励](./物品与任务奖励.md)；战斗目标和终局见 [战斗与结局](./战斗与结局.md)。
