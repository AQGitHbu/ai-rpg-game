# 行动裁决

## 职责

行动裁决把客户端提交的固定选择或焦点 NPC 自定义输入转换为受支持的 `Action`，校验当前世界状态，并在一次规则提交中产生状态、任务、关系、知识、战斗和结局结果。AI、UI 和玩家原文都不能直接写入事实。

## 当前契约

- 固定选择使用当前 revision 的 opaque `choiceToken`；客户端不提交 `Action`、`actionKey`、effect、事实 ID 或候选事件 ID。
- 自定义输入携带文本和焦点 NPC ID，由 `convertInteraction` 映射为中性 `talk/ask`；原文供当前叙事表达，不做独立 intent provider 调用，也不直接声明规则结果。
- 请求带独立 `actionId` 与 `expectedRevision`。非法、过期、重复、已消费或结局后的请求零写入。
- 玩家操作由 `talk`、`move`、`take_item`、`give_item`、`attack` 和 `battle_action` 承载。Action union 中的 `explore`、`investigate`、`ack_prologue`、`freeform` 保留内部或流程用途，不代表泛化探索、调查或任意意图的公开玩家入口；自由输入实际转换为 `talk/ask`。
- NPC 对话、赠物、明确 NPC 任务和共同战斗的后果通过 Entity Store 的 `EntityMutation` 写入；关系 signal、知识传播、债务和对话完成由规则计算。
- 每个成功规则回合通过 `commitEventDrafts` 追加 committed event，`StoryState.memory` 从完整 event ledger 重建。CAS 失败不补写事件、记忆或玩家行动。
- 新的 `discover_fact` 目标在规则边界自动确认；`investigate` 仍保留在 Action 与 bundle trigger 中，供显式 offline fixture 或内部规则测试消费，当前生产 read model 不生成调查方式 token，也不承诺旧存档迁移。
- 生产续接只消费已批准并整包发布的场景步骤。`PreparedContinuationState` 只用于显式 offline fixture；生产线性动作不能因缺少步骤而调用 provider 或生成替代剧情。

## 必要流程

```text
Interaction → requestParser / actionConverter → validateAction / resolveByType
→ quest、NPC、战斗、结局与事件 reconciliation → TurnResolution
→ 单次 StateCommit / repository CAS → GameSessionView
```

规则回合最多执行一次 `applyState`。正式 NPC 回合可写入白名单 `PendingNarrativeJob`；叙事审批与写回是独立的 narrative CAS。`move`、物品、战斗和回退导航只消费已发布场景或规则场景，不重复执行 AI 生成。

## 主要源码和验证

- `src/game/domain/action.ts`、`src/game/domain/eventLedger.ts`、`src/game/domain/episodicMemory.ts`、`src/game/domain/entity/entityStore.ts`
- `src/game/application/actionConverter.ts`、`src/game/application/performTurn.ts`、`src/game/application/stateCommit.ts`
- `src/game/gameplay/rpg/ruleEngine/`、`src/game/gameplay/rpg/dialogue/`、`src/game/gameplay/rpg/entityWorld/`
- `src/game/application/buildChoiceMap.ts`、`src/game/application/gameSessionView.ts`
- `src/app/api/game/actions/route.ts`
- `src/game/application/performTurn.test.ts`、`src/game/gameplay/rpg/ruleEngine/index.test.ts`、`src/game/application/testing/investigationChoiceJourney.test.ts`、`src/game/application/testing/npcContinuityJourney.test.ts`、`src/game/application/testing/episodicMemoryJourney.test.ts`

## 按条件关联文档

- 地图入口与任务目标见 [探索与任务推进](./探索与任务推进.md) 和 [地图与地点冒险](./地图与地点冒险.md)。
- NPC 对话与叙事生成见 [NPC 对话驱动叙事场景触发](./NPC对话驱动叙事场景触发.md)。
- 战斗和终局见 [战斗与结局](./战斗与结局.md)。
