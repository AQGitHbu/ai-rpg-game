# 行动裁决

## 状态

Phase 3 已规划，尚未实现。当前只有 Phase 2 的创建、读取与开场 read model；没有玩家行动入口、resolver 或续存档写入。

## 设计边界

- 玩家提交的是结构化意图，而不是已经发生的事实。
- 只有 `src/game/gameplay/rpg/actions/` 的纯规则 resolver 可以产出下一份 `GameState` 和领域事件。
- UI/API 只能通过 `@/game/application` 门面调用动作 use case，不能直接导入 domain、gameplay 或 persistence。
- Phase 3 只做固定选项的 `observe`、`talk`、`investigate`；自由输入、AI 意图解析、移动、任务推进、战斗与物品使用均不在本阶段。
- 行动结果必须先以原子 compare-and-swap 写入 SQLite；版本冲突或拒绝的行动不得留下事件或部分状态。

## 入口

- 玩家规则：`docs/策划文档/AI生成RPG_MVP.md` 第 6 节
- 执行 Plan：`docs/superpowers/plans/2026-07-27-mvp-phase-3-deterministic-action-loop.md`
- 上游存档：`src/game/application/server/persistence/`

## 实现后必须记录

- intent、稳定拒绝码、事件类型及各自允许的状态变化；
- SQLite revision / migration 与原子写入事实；
- application facade、HTTP route、UI 组件和测试入口；
- Phase 3 未覆盖的行动类型与下一阶段边界。
