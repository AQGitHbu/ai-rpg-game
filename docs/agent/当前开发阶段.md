# 当前开发阶段

> 当前阶段：结构化 Event 与 Episodic Memory
> 状态：已规划，待执行（Plan 3 已验收并合入 main；当前生产基线仍为 WorldState v4 / StoryState v7 / EntityStore v2）

## 本阶段目标（待执行）

- 唯一执行 Plan：`docs/superpowers/plans/2026-09-02-structured-events-episodic-memory.md`（总 Spec 的 Plan 4，对应 NAR-08、NAR-09、NAR-14、NAR-17）。
- 目标：把弱类型事件数组升级为带稳定 ID、回合、参与实体、地点、因果、显著度和来源的不可变事件账本；所有规则、世界演化和场景写回统一走一个事件提交入口。
- 目标：从完整账本确定性重建 Episode、近期场景记忆和 NPC 接触索引，并按 Event/任务/实体/地点/因果/显著度/近期性检索旧事；下一幕只接收有界 memory cards，不接收完整账本。
- 目标：NPC knowledge、relationship evidence、interaction 与台词引用升级为真实 Event provenance；actionId 继续负责幂等，不再冒充长期事件证据。
- 版本目标：`WorldState.version=5`、`StoryState.version=8`、`EntityStore.version=2`；旧 v4/v7 开发存档明确 `UNSUPPORTED_RECORD`。
- 明确排除：Living Outline/Arc/Story Thread（Plan 5）、Arc-aware planning（Plan 6）、分段 ledger/snapshot/归档/向量索引（Plan 7）、十小时长篇（Plan 8）、misinformation、新 provider 调用、API/行动/CAS 变化和共享 package 改动。

## 上一阶段 Plan 3 实现摘要（已验收并合入 main）

- `WorldState.version=4` / `EntityStore.version=2` 已成为当前严格存档版本；旧 v3/v1 返回 `UNSUPPORTED_RECORD`，坏嵌套组件返回 `ENTITY_STATE_INVALID`。
- `NpcEntityRecord` 以 `identity`、`position`、`dynamicState`、`knowledge`、`relationships`、`history` 固定分层。人格 anchors 与 typed goals 在开局/动态创建时经审批写入；普通行动没有 identity 更新通道。
- 知识条目按 canonical `FactId` 去重，保留 certainty、disclosure 与 `initial_world`/真实 `actionId + turnNumber` provenance；FactChange 仅向显式 audience 传播，`npc_revealed` 要求真实 `sourceNpcId`。
- NPC→玩家与 NPC→NPC 都是有向关系边。封闭 signal 表负责有限速的四维变化、stage 邻接迁移、evidence 幂等与 debt/promise 状态；AI 不能提交数值 delta 或通用 patch。
- `NpcSpeechAuthority` 统一 opening、narrative bundle、prepared continuation 和 scene write-back 四条 NPC 台词引用审批路径；只允许当前 speaker 的可披露 Fact 与自己的 interaction actionId，拒绝重复、越权、未披露引用。
- 赠物、明确 NPC 任务和共同战斗读取同一 NPC 投影；同地点同伴最多一名按 ID 确定性参战，失败/撤退恢复战前全组件，胜利才写 `fought_together`。
- `npcContinuityJourney` 已证明离线五幕、至少 18 个成功回合、至少 3 次 reload、NPC-to-NPC seed、知识隐私、赠物、任务、战斗失败/胜利与最终结局；grounding journey 不再跳过物品或战斗断言。
- Chrome 真实 API 中篇回归已从新游戏完整走完五幕、战斗和终幕立场，结局可在刷新后恢复；本轮修复覆盖 canonical Entity Store speech authority、终点抵达 NPC 台词约束、可选调查方式坏字段隔离以及终幕 `endingPair` 保留。
- Plan 3 没有实现 Event/Episode 与稳定 `eventId`；这些是当前 Plan 4 的实施范围。Living Outline/Arc/Story Thread、长篇 segmented storage、Identity Anchors 运行期更新或新增 provider 调用仍不在当前生产能力内。

## 上一阶段（实体与组件世界状态基础）实现摘要

- `WorldState.version=4` 的 `entityStore` 是 8 类实体的唯一权威事实源；玩家、地点、NPC、物品、事实、任务、敌人、阵营与位置/持有/解锁/到访/击败索引均由 `projectEntityStore` 确定性派生，不存在动态 component 字典或任意 JSON Patch。
- 封闭的规则可信 `EntityMutation` 是唯一写入语言，整批校验通过后一次投影；候选事件、世界演化物化与揭示游标均经 mutation 写入，战斗检查点保存完整战前 store 并在失败/撤退时从其重建兼容投影。
- SQLite 持久化边界严格解析 v4 store、校验实体/世界引用与投影一致性；旧版本存档返回 `UNSUPPORTED_RECORD`，损坏 v4 实体状态返回 `ENTITY_STATE_INVALID`。
- 不可信 `ProposedEntityCommand` 已有封闭 parser、allowlist 审批与到可信 mutation 的纯映射，但尚未接入 provider 输出，AI 对既有实体没有更新权限。
- 应用层 Entity 规则闭包上下文投影已接入 scene/world/bundle 三条 narrative context 路径，保持既有 DTO、预算、无正文 manifest 与一次 provider 调用。
- 详见 `docs/agent/实体与组件世界状态.md`。

## 上一阶段（叙事上下文编译器）实现摘要

- `application/server/ai/narrativeContext/` 已建立纯函数上下文块 IR、确定性预算编译、稳定渲染和无正文审计 manifest：统一 `NarrativeContextBlock`、固定 authority/slot 顺序、mandatory 永不因预算被裁剪、optional 按优先级入 8k token 预算。
- 当前 v7 生产决策链由 `liveNarrativeBundleSource` 调用 `compileDecisionNarrativeContext`，一次编译同时投影 Story Contract、幕/张力/节奏与预算、近期 beats、本轮强制节拍、焦点 NPC 五条结构化交互、公开事实、当前实体索引、叙事包图与完整 output contract。历史 scene/world 源仍使用 `compileSceneNarrativeContext` / `compileWorldNarrativeContext`，但不是 v7 生产决策链。
- 生产 `narrative_bundle` 决策调用的审计 `narrativeContext` 只保存 manifest 元数据，不保存额外 Prompt 副本；初始化 opening 分支与 `intent` 尚未迁入该编译协议，Plan 4 的 Event/Episode 与 Plan 5 的 Living Outline 记忆仍属后续边界。
- 上一阶段未迁移存档、未增加 AI 调用、未改变规则结算、战斗失败回滚、场景/world 审批链，短篇/中篇可完成链保持不变。

## 唯一执行入口

- 机器可读状态：`docs/agent/current-phase.json`（当前为 `planned / not_started`）
- 当前 Plan：`docs/superpowers/plans/2026-09-02-structured-events-episodic-memory.md`
- 目标分支 / worktree：`codex/structured-events-episodic-memory` / `.worktrees/structured-events-episodic-memory`
- 修改仓库：仅 `ai-rpg-game`；本阶段不修改 foundation。
- 启动命令：从干净 main 运行 `npm run phase:start`；尚未创建目标 worktree、尚未开始实现。

## 当前生产基线

- 开局只生成可完成的开局切片：故事契约（幕数/中心冲突/两个抽象结局方向）、世界前提、玩家、序幕、1 个起始地点、1 名带稳定人格 anchors/typed goals 的开场 NPC、1 条活动主线与开场事实；不再生成完整未来实体蓝图。
- 每个 ready 场景走一次场景表演调用：分段旁白逐段对应服务端强制节拍（≤8），`objectiveLink` 与权威当前目标一致；旁白可用 `referencedEntityIds` 表达 grounding，但不要求逐字复述目标标签；焦点 NPC 只收到同一 `NpcSpeechAuthority` 投影的隔离记忆（最近 5 条结构化交互、回应政策、允许披露事实，不含玩家原文与未披露私密正文）。
- 世界演化是按需的可选调用：仅 `EvolutionNeed !== none`（幕推进/节奏/终局对）时触发；AI 只提案，审批后服务端铸造 ID，与场景写回同一受控 CAS 原子持久化。
- 终幕按 trust/doubt 两个抽象方向具象化互斥结局对；不存在预生成的隐藏任务图。
- 城镇层确定性重建：几何与剧情建筑 slot 开局预分配，运行时 NPC 绑定 slot；无 `/api/game/town/ensure` 路由、无 AI 生图。
- 无需玩家决策的 provider job 在创建新局或正式 NPC 回合后立即后台排队：序幕阅读期间静默预生成，协调器按 `gameId + jobId` 去重，客户端对每个新 job 只 ensure 一次、随后 GET 观测；prepared/rule action 不创建 pending。序幕确认状态在并发 scene CAS 中单调保留。
- provider 场景候选不足时生产链将其归类为 AI 响应/审批失败并持久化 failed；线性动作不会以候选不足为由补发 provider。明确 NPC talk 行动首击即提交，不制造二次确认或假等待。离线 journey 通过显式 fixture source 覆盖规则链。
- 保持六个 `/api/game/**` route、单次规则 CAS 与独立 scene CAS；runtime narrative state 使用 `ready` / `provider_pending` / `provider_failed` 判别联合，failed 跨 reload 保留，现有 ensure route 接受 `{ "retry": true }` 以同一 job 手动重试；正式产品只开放短篇与中篇。

- NPC 对话、调查事实传播、赠物、明确 NPC 任务与共同战斗的后果都通过 Entity Store 的细粒度 mutation 写入；兼容 `NpcMemory` 只读派生，不与组件并行更新。

## 生产运行链

```text
CurrentGameScreen / AdventureGameShell / TownLayerScreen
  → POST /api/game/actions
  → compositionRoot.performTurn
  → performTurn（规则审批、任务/关系/知识/结局、派生 ObjectiveTransition + 强制节拍）
       ├ provider：单次规则 CAS 写入白名单 PendingNarrativeJob
       │    → BackgroundEnsureCoordinator（gameId + jobId 去重）
       │    → generatePendingNarrativeBundle
       │         └ narrative bundle（可选 world delta + current scene + continuation graph）
       │            → approveNarrativeBundle
       │            → GameRepository.applySceneWriteBack（独立 CAS）
       ├ prepared：规则结果 + prepared scene/token/continuation consume 一次 CAS
       └ rule-only：规则结果 + source="rule" scene 一次 CAS
  → GameSessionView（只投影当前 scene、opaque choices 和 opaque jobKey）
```

生产 API 精确为六个 route：

1. `POST /api/game`
2. `GET /api/game/current`
3. `POST /api/game/actions`
4. `POST /api/game/narrative/ensure`
5. `POST /api/game/prologue/ack`
6. `DELETE /api/game/dev/current`（仅 development）

不存在旧路由、重定向、兼容 facade 或并行仓储，也没有 `town ensure` 路由。SQLite 只使用 `game_records` 与 `current_game`；既有本地开发存档不迁移，升级后必须清档重开。坏数据和不支持的 record 只返回稳定分类，不能静默重置。

`POST /api/game/narrative/ensure` 只接受 `{}` 或 `{ "retry": true }`：普通请求只观察/恢复 pending；failed 请求返回稳定 `AI_GENERATION_FAILED + failureKind`，只有 retry 请求才以 revision/jobId CAS 将同一 job 恢复为 pending。

## 玩家选择闭环

- 固定选择请求：`{ actionId, interaction: { kind: "fixed_choice", choiceToken }, expectedRevision }`。
- 自定义输入请求：`{ actionId, interaction: { kind: "free_text", text, targetNpcId }, expectedRevision }`。
- 浏览器每次提交生成新的 UUID；同一 NPC 的连续自定义输入不会被错误去重。
- 焦点 NPC 对话视图严格提供两个服务端批准的固定选择；仅焦点 NPC 开启一个自定义输入。
- 自定义输入由服务端意图解析器转换成受支持 Action；玩家原文只存在于 `PendingNarrativeJob.utterance`，不进入长期记忆、事件账本或日志。
- tampered、stale、已消费 token 均零写入；AI/fixture 不能直接修改规则状态。

## 可玩性证明

- `dynamicMaterializationJourney.test.ts`：开局恰 1 地点/1 NPC/1 主线；首次对话后下一次写回具象化新 NPC 与（地点或物品），下一场景提及已批准名称；地图 → 城镇 → 建筑场景、物品、战斗与结局 ≥15 成功回合、三次 repository reload。
- `narrativeGroundingJourney.test.ts`：提交当前话语 → 下一场景出现直接 NPC 回应节拍；hostile/trusted 档位给出不同回应政策；物品/任务/战斗后每个场景覆盖全部强制节拍，且 `objectiveLink` 等于 HUD 当前目标。
- `storyDivergenceJourney.test.ts`：同 seed 的“支持/质疑”两条分支各自具象化可完成内容、保留隔离 NPC 记忆并抵达不同结局方向——stock 确定性源为结局对附上规则要求（信任需关键 NPC 亲和度 ≥10，质疑 ≤9），支持分支亲和度走高命中信任、质疑分支亲和度走低命中质疑，全程离线裁决；分支 replay 深比较 `WorldState + StoryState` 完全一致。
- `mediumActJourney.test.ts`：中篇（5 幕）离线可完成性证明——从第 1 幕经真实幕链（交谈→具象化→传讯人·N→具象化…）推进到第 5 幕终点并解析结局；断言 `currentAct == targetActs`、结局已具象化且已解析、预算各维度仍在软上限内。
- `foundationJourney.test.ts`：驱动至少 15 个成功回合，跨多次 reload，覆盖固定 NPC 选择、自定义输入、旅行、拾取、探索、战斗和结局。
- `npcContinuityJourney.test.ts`：离线五幕至少 18 个成功回合、至少 3 次 reload，覆盖 NPC-to-NPC 有向 seed、FactChange audience 隔离、赠物/任务、战败恢复、共同战斗胜利和最终结局。
- seed 回归证明相同 seed 生成相同结构，不同 seed 至少改变世界名、地点或 NPC 的结构化组合。
- AI 不可用或响应不合法时保留已提交规则状态，场景持久化 `failed` 并显示 stable failureKind；玩家点击“重试”后复用同一 job。完整无 AI 旅程只由显式 fixture source 驱动，不代表生产 AI 失败时仍可通关。

## 兼容与容量边界

- 本阶段允许破坏性重建：旧 HTTP 契约、旧存储表、旧 application/UI 链和类型隔离均已删除，不提供迁移或兼容层；旧蓝图聚合与全量世界生成候选已移除。
- 当前 SQLite 以单条 `worldState + storyState` JSON record 持久化整局；短篇/中篇是正式支持范围。
- 长篇/开放式支持必须另立 segmented ledger Spec/Plan，覆盖 append/event 分段、snapshot/cursor、归档、回放、事务和迁移后，才可进入 UI 或产品承诺。

## 验收命令

```text
npm run check:standards
npm run typecheck
npm run lint
npm run test:boundaries
npm run test:fast
npm run test:game-domain
npm run test:game-gameplay
npm run test:game-application
npm run test:components
npm run test:app
npm test
npm run test:foundation-journey
npm run journey:foundation
npm test -- src/game/application/testing/npcContinuityJourney.test.ts src/game/application/testing/narrativeGroundingJourney.test.ts src/game/application/testing/investigationChoiceJourney.test.ts src/game/application/testing/dynamicMaterializationJourney.test.ts src/game/application/testing/mediumActJourney.test.ts src/game/application/testing/storyDivergenceJourney.test.ts
npm run build
npm run phase:status
```

历史 Spec/Plan 与 dated fixture 可保留其版本值；它们不再定义生产文件、接口、路由或运行链命名。

2026-09-02：Plan 3 已在 main 完成代码、离线旅程、真实 API 中篇回归、全量测试与生产构建验收；原目标分支/worktree 已收尾。Plan 4 已形成详细实施计划，阶段状态切为 `planned / not_started`，尚未创建 worktree。
