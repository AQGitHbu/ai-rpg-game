# 当前开发阶段

> 当前阶段：Phase 10：运行时 AI 导演与场景表演（单仓）
> 状态：已收尾（completed / merged； 已合并回 main，worktree 已清理，下一阶段待正式立项）

## 收尾后的开发主线

- Phase 10 成果已全部合并回 `main`（merge `448777e` 及后续 phase10 完整旅程测试）。
- 其后主线在 `main` 上继续收口：town 层主循环实装 + 真实 AI 旅程录制/回放（merge `00e7716`）、日志 facade 迁移（`3d39796`，plan `docs/superpowers/plans/2026-07-31-rpg-logging-facade-migration.md` 已执行）。
- 候选后续计划（均尚未立项为正式阶段；立项时按流程更新 `current-phase.json` 为 planned 并创建对应 worktree）：
  - `docs/superpowers/plans/2026-07-31-rpg-layered-architecture-development-standard.md`（分层架构开发规范）；
  - `docs/superpowers/specs/2026-07-30-blueprint-dynamic-evolution.md`（蓝图动态演进，待实施，配套 plan 尚未编写）。

## 唯一执行入口

- 机器可读状态：`docs/agent/current-phase.json`
- 执行 Plan（唯一）：`docs/superpowers/plans/2026-07-30-mvp-phase-10-runtime-ai-director.md`
- 设计 Spec：`docs/superpowers/specs/2026-07-30-runtime-ai-director-scene-performance-design.md`
- 系统 Agent 文档：`docs/agent/运行时AI导演与场景表演.md`
- 修改仓库：仅 `ai-rpg-game`（`sharedInfrastructureChangeAllowed: false`，零 foundation 改动）
- 验收命令：`npm run lint`、`npm run typecheck`、`npm test`、`npm run test:fast`、`npm run build`、`npm run phase:status`（全部离线）

## 阶段目标

```text
初始规则状态
→ 规则状态 / pending 场景任务一次 CAS 保存
→ 前端 ensure + 轮询
→ director AI → 规则批准
→ writer AI → 规则批准
→ 当前 NPC AI（最小知识）
→ 场景 CAS 保存为 ready
→ 两个服务端批准固定选项
→ 玩家提交 choiceToken
→ 既有规则裁决
```

## 阶段范围

- director / writer / npc 三个独立 source、prompt、schema 与最小上下文投影。
- director 可读全局剧情约束；writer 可读批准计划与相关真相；NPC 只读自己的档案与获准 known facts。
- 两个选项必须映射两个不同且当前合法的既有 `AvailableAction`。
- 客户端只提交 choiceToken 和 revision，不获得 actionKey 或隐藏 ID。
- 新游戏和成功选择先持久化 pending；后台任务生成并 CAS 保存初始/下一 narrative scene。
- AI 任一阶段失败后整场确定性 fallback，不改变规则结果。
- SQLite 单次 CAS、stale 丢弃、reload 一致性与 battle/ending 旁路。
- 复用现有 AI transport/config；新增离线 fixture 回归和显式 opt-in 真实 smoke。
- 继续使用本地 SVG；美术接口仅保留设计扩展点。

## 最新验证事实

- 真实 director / writer / NPC 分请求链路已接入；每角色最多三次有界尝试，只重试当前失败角色。
- `narrative_choice` 已可通过正式规则 facade 进入 `start_battle`，战斗数值和胜负仍完全由程序裁决。
- 已建立双模式完整旅程：
  - 真实认证：`RUN_REAL_AI_JOURNEY=1 npm run smoke:ai:phase10-journey`；
  - 零网络回放：`npm run journey:phase10`。
- 2026-07-30 真实完整旅程通过：14 次真实角色调用、6 次剧情选择、2 次 NPC 台词、3 个首次到访地点、1 个首次交谈 NPC、1 个道具、boss 战和成功结局；随后即时 replay 通过。
- 已提升真实录制到 `data/fixtures/phase10-journey/v1/`，日常测试不再调用 API。
- 场景生成已改为持久化 pending + 单飞后台任务 + 750ms 客户端轮询；进程重启后 POST ensure 可恢复。
- 非战斗主界面保持地图优先；AI narrative 只在玩家进入当前地点后显示。开发环境可使用 Phase 10 固定离线旅程基线开局，保存为 offline 模式且零 AI 调用。
- 运行时仍不创建新的 blueprint ID；动态新增 ID 必须作为后续独立的受审批蓝图扩展阶段。

## 明确不做

- 玩家自由输入与意图解析 AI；
- 运行时新增 NPC、地点、物品、任务或修改蓝图；
- AI 修改数值、奖励、战斗、任务、地图、背包或结局；
- 多 NPC 独立表演、长期记忆、关系数值和自主日程；
- streaming、SSE、AI 图片、语音和视觉描述生成；
- foundation、共享 UI 或 transport 修改。

## 历史阶段事实

Phase 8 非战斗游戏视窗已实现；Phase 9 场景化战斗视窗已实现。Phase 10 只在这些既有 UI、规则、AI transport 和持久化能力之上增加受约束运行时叙事，不重做地图或战斗。
