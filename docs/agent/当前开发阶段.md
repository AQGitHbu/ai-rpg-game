# 当前开发阶段

> 当前阶段：v2.1 基础修复（Foundation Remediation）R6：Expansion 生产闭环、Action 闭合与完整分化旅程
> 状态：R6 已实现（implemented；worktree `.worktrees/v2-foundation-remediation`，分支 `codex-v2-foundation-remediation`）

## 阶段目标（v2.1 生产链）

- 把叙事/行动链路收敛到 v2.1 生产链（`/api/v2/**` + `compositionRootV2` + `performTurn` + `generatePendingSceneV2`），v1 仅作历史参考（另立删除 Plan，不在本 Spec 内大规模删除）。
- R6 聚焦：Expansion 纯规则完整性与 live 源注入、Action union 全闭合、15 回合完整旅程、同 seed 分叉与多结局证明、持久化规模边界与长篇门禁、文档收口。

## 唯一执行入口

- 机器可读状态：`docs/agent/current-phase.json`
- 执行 Plan（唯一）：`docs/superpowers/plans/2026-08-08-ai-rpg-v2-foundation-remediation.md`
- 设计 Spec：`docs/superpowers/specs/2026-08-08-ai-rpg-v2-foundation-remediation-spec.md`
- 修改仓库：仅 `ai-rpg-game`（共享基础设施沿用既有 `@ai-game/*` package，本阶段零 foundation 改动）
- 验收命令：`npm run lint`、`npm run typecheck`、`npm test`、`npm run build`、`npm run test:boundaries`、`npm run test:v2-foundation-journey`（全部离线）

## R5 实现事实

- **Task 22 对话记忆闭环**：`NpcInteraction` 改为 v2.1 结构化契约（turnNumber/actionId/dialogueAct/topicSummary/learnedFactIds，outcome 扩展 mixed）；玩家原文绝不进入长期 summary；`updateNpcMemory` 同 actionId 不因错误重试追加两次（CAS 失败路径零写入）、关系 clamp、历史保留最近 10 条。
- **Task 23 知识传播来源**：`FactChange` 引入封闭 `source`（scene_witness/player_told/npc_revealed/public_broadcast/faction_shared）；`propagateKnownFacts` 拒绝非法 source/不存在 fact/不在场 audience，只按显式 audience 传播，绝不自动扩散给同地点所有 met NPC；investigate 行动不再自动让当前地点所有 NPC 知晓。
- **Task 24 SceneGenerationContext 最小权限 DTO**：新增 FactCard/NpcSceneContext/PlayerSceneSummary/LocationSceneCard/BudgetSummary/LegalActionCandidate；每个 NPC context 只含自己的 known/hidden fact cards、最近规则摘要、relationship/emotion/goals、forbidden knowledge 索引；私密事实只出现在对应 NPC 自己的 hiddenFactCards，绝不进入 public/scene-visible facts。
- **Task 25 场景包审批**：`approveScenePackage` 校验旁白非空、台词 NPC 在场、NPC 不使用 forbidden fact、选项不语义重复、选项目标合法；核心非法整场 fallback（fallback 同轨审批）；`generatePendingSceneV2` 接入完整审批 + 候选事件池审批。
- **Task 26 read model 隔离**：`gameSessionViewV2` 任务 discover_fact 目标在事实未发现时只显示中性目标'发现秘密'，绝不泄漏 fact.text/FactId；NPC 对话选项只挂焦点 NPC，freeInput 仅焦点开启。

## R6 实现事实

- **Task 27 Expansion 纯规则完整性**：trigger 覆盖 entity_not_found/低张力/任务角色缺口，并新增 reuse（已有合适实体时复用不新建）与收束信号（climax/resolve/soft_max/hard_limit/budget_exhausted）；UNKNOWN_NPC 仍创建以满足明确行动；approve 支持同批 ID 唯一、跨批引用二次验证、item/enemy/fact 消耗 events 预算；apply 新地点立即解锁且双向连接、NPC 挂 npcIds 索引、物品挂 availableItemIds、不覆盖既有字段。
- **Task 28 live ExpansionSource**：新增 `liveExpansionSourceV2`（AI 提案→纯解析/校验/引用过滤→失败回退空提案，零敏感日志）；`createV2ExpansionSource` factory 按 AI 配置选 live/fixture，compositionRootV2 不再直注入 fixture；编排抽到 `application/expansionProposer.ts`（条件触发才调用 source、最多一次重演算、source 失败零写入降级）。
- **Task 29 Action union 闭合**：从生产 `Action` union 移除无规则实现的 use_item/give_item/interact/accept_quest/narrative_choice（不再保留永远 INTENT_NOT_ROUTED 的公开类型），并同步 V2 消费方（approvedChoice/approveSceneChoices/performTurn/pendingNarrativeJob/eventKindForAction）；新增 `SUPPORTED_ACTION_TYPES` 支持矩阵守卫；explore→`location_explored`、rest→`player_rested` 无状态行动也产生主事件。
- **Task 30 15 回合离线旅程**：`v2FoundationJourney.testutil`（内存 CAS repo + createGameV2）驱动多回合 performTurn，断言每成功回合单次规则 CAS、reload 确定性、pending 阻断语义；`scripts/v2FoundationJourney.mjs` + `npm run test:v2-foundation-journey` / `journey:v2-foundation`。
- **Task 31 同 seed 分叉证明**：`v2StoryDivergenceJourney` A/B（support vs challenge）同一开局对关键 NPC 产生同回合结构化分化（affinity/summary 不同）、分支重复 replay 完全确定、自由输入两种语义文本分类与记忆分化且越权声明不改事实。
- **Task 32 持久化规模边界**：`sqliteGameRepositoryV2` 80 回合连续 CAS + reload + 大小基线用例；`docs/agent/剧情连续性与结构化记忆.md` 明确 v2.1 正式支持短篇/中篇，long/open 启用前必须另立 segmented ledger Plan，UI/产品不得声称无限长期记忆。

## 验证（R6 收口）

- `npm run typecheck`：0 错误；`npm run test:boundaries`：91 过。
- `npm test`：分层全绿（domain/gameplay/application/components/boundaries），`npm run test:v2-foundation-journey` 通过。
- `npm run build`：✓ Compiled。
- 提交：R6 共 7 个提交（Task 27-33），分支 `codex-v2-foundation-remediation`。

## v1 类型债隔离（跨 R5 前置）

- 因 R1（Task 2）把 `narrative.generation` 改为 `PendingNarrativeJob` 结构，v1 legacy 叙事/行动链路产生类型漂移。v1 业务 re-export 已从 `index.ts` 拆分到 `index.v1.ts`，并将 v1 链路文件从主 typecheck/build 门禁排除（保留运行时测试）。v2.1 为生产链路，v1 仅作行为参考。

## v1 明确不做（历史参考链）

- v1 叙事/行动链路（`performAction`/`handleNpcDialogue`/`orchestrateNarrativeScene` 等）保留作行为参考，删除由独立 v1 删除 Plan 执行，不在本 Spec 内大规模删除。
