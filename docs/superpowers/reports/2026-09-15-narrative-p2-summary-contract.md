# P2 摘要选择契约修复与真实恢复验证

## 结论

本次摘要选择契约修复得到真实证据支持：固定来源回归 `p2-memory-contract-02` 使用3次正式行动、16次真实 HTTP，在两个不同 player preparation job 中发布两批摘要（水位15、29），随后作者实际请求与玩家回忆正文均保留开局交付条件。独立 SQLite 严格回放匹配16次响应和5个状态检查点，源存档哈希未变。

这是**固定来源的摘要发布与恢复回归**，不是新建完整故事，也不覆盖整套P2验收。原话通过概览保留，不是概览省略后重新检索的实证；没有执行最终交付、结局、两臂或UI。

## 改动范围

`liveNarrativeMemorySummarySource.ts` 在同一次调用中用同一个 limits 对象渲染提示并校验输出：批次最多4条History和4个Event，概览最多8/8。说明上限不是填满要求，优先选择动机、约定条件、关键变化和未解决事项，区分亲见/转述/推测并省略重复。

保留50/10、来源权限、原文不可改写、请求预算、缓存与失败回退。超限仍拒绝，未用静默截断制造通过。没有新增Entity、模型角色、剧情规则或检索架构。

新增两类提示与解析一致性回归：合法上限通过，10条History超限拒绝，实际发出的提示包含对应History/Event上限。相关17项测试、typecheck与131项边界检查通过。

## 冻结来源与实际路径

新实验复制 `p2-memory-01/B.sqlite` 的第四幕安全点（56条有效History、零摘要）到独立数据库。旧协议、失败manifest与源数据库保持；新协议冻结源库/状态/原协议hash、修复后代码指纹、实验脚本hash和固定输入。最多6行动/100 HTTP/60分钟，模型仍为 `ai-slg-game-model`、输入估算上限64,000。

复用正式 composition、performTurn、ensure、SQLite摘要仓储及既有录制/回放。固定输入为尚未执行的 `4-evidence`、`4-decision`；两批发布后立即执行原协议的旧事追问，不追加同类问题。仅后续生成使用修复代码，旧故事作为明确标注的来源，不冒充旧失败样本重新通过。

| 实际行动 | 摘要结果 | 主要证据 |
| --- | --- | --- |
| 第四幕证据追问 | player发布水位15 | `job_79d32dbd-4149-416d-bbc7-b52d61a67eef` |
| 第四幕决定权限追问 | player发布水位29 | `job_1865a3d6-ce49-4636-a290-d94773f60cc7` |
| 开局旧事追问 | 新批次未发布，保留水位29 | `job_1eec2754-0439-4fae-a0fc-8a1107ce6fc2`，实际原文恢复成功 |

产物：`artifacts/narrative-p2/p2-memory-contract-02/` 的 protocol.json、run.mjs、live/result.json、live/review.json、请求审计与 memory.runtime.json；replay/ 保存独立重放结果。实验脚本是本次冻结回归材料，不是新生产入口。

## 回答保真与作用

开局固定原话要求：秋分前三日晨钟后、东水祠前、里正或代行者接收；信可公开阅读、原次序不能改，不替人添加让步。实际作者请求完整包含该History ID与正文，路径为 `overview`。玩家回忆正文逐项保留这些条件，没有将交付与接受分水方案混为一谈。

守门人表示未亲历最初委托，不替委托人作证；本次对应NPC判断请求未包含开局整段原话。这个样本支持“玩家可以回想旧话，当前NPC仍按自身经历回答”的区分，不等于所有秘密/角色权限都已实跑覆盖。

回忆为当前决定提供了原始交付边界，能帮助理解守门人与指定接收人的权限区别；本次没有执行后续选择，因此不能声称它改变了结局或证明整体游戏性提升。

## 剩余限制

第三次批次选择合法，未复发数量超限，但全部叶选中来源累积后的概览输入估算为6,875 tokens，超过现有6,000概览源预算，编排因此没有请求新概览、没有发布或推进水位。此前两批缓存与原文仍可使用，回忆正常完成。此处是概览源容量限制，不是模型1M上下文不足，也不能归为本次选择契约仍失败；保持为P2后续覆盖事项，本轮未放宽预算。

仍需分别取得概览省略后恢复、同源两臂、UI重载接续与完整故事质量证据。此次成功不抹除 `p2-memory-01` 的失败，也不代表P2整体通过。
