# 完整规划内容传递修复验收

## 基线与范围

从 `79c41172ae7b305339af90ab4783b3967f043c8f` 重做。放弃的修改已备份到 `tmp/abandoned-dialogue-repair-37cd2995-712c-49fe-99bd-3addc4f3047a`，包含二进制 patch、文件副本及清单。未提交、合并或修改用户存档、AI 配置和 `.foundation`。

确认问题及对应修复：

1. 完整意图被压缩成 intent/fact 标签，导致协助、态度和问题对象变形：专属 `brief` 经引用权限检查后原样传给表达器。
2. 全部 focus 被强制表达，未知回答也要重讲背景：`contentFactIds` 区分必须正文与话题背景，答案和先求证条件仍必须覆盖。
3. 同一 NPC 回应拆成多个生成任务：规划一个完整 character 单元，可在一次返回中自然分段。
4. 本轮选题收窄时裁断前文，规划又只见上一轮：前文先按完整权限保留，并为规划提供有界同 NPC 历史；历史不是新事实来源。
5. 规划将已有背景当成补写具体答案的许可：规划提示在输出位置明确授权事实依据、未知范围及禁止重讲背景，由真实正文验收效果。

不增加表达模型审核。待确认的 UI 整页重复问题未处理。新知识披露仍沿用基线已有审核。

## 真实 API 方法

从原失败任务的冻结快照 `tmp/dialogue-coherence-198fb4b2-0863-4bba-afe1-5f01df36d20f/game.sqlite` 复制独立数据库，恢复其中原问题提交前的任务输入；重放原问题，然后连续选择三轮。仅在副本把同 NPC 的 `requiredTurns` 延长到 8，避免途中进入无关下一幕。使用本地已有 AI 配置；真实调用，不使用离线正文。

测试检查接口成功、发布和 revision 前进；语义结论另外逐轮对比规划 brief、NPC 实际正文、两个选项和调用审计。源快照在测试前后校验 SHA-256。

## 调试样本

`tmp/planner-content-dialogue-f49e38c4-c55e-4e3b-bb65-877ff333b623`：四次 NPC 回应、18 次 provider 调用。接口链路成功，但语义不通过：规划明确要求反复讲 fact_1，随后编造刀客来去方向。该样本不算验收成功；修复转向规划内容依据和隔轮历史。每次 NPC 回应已只调用一个 character 单元。

## 前次验收（继续修复前）

**工程检查通过，真实对白语义未完全通过，不能标记全部修复完成。**

最新样本：`tmp/planner-content-dialogue-07243b31-5835-4afd-a4de-838237a1fa99`，`result.json` 保存每轮输入/输出，`audit/events.jsonl` 保存真实请求/响应。原问题重放加三轮选择全部发布成功，耗时约 55 秒，共 17 次 provider 调用：planning 5 次（含一次重试），narration、character、choices 各 4 次。每次 NPC 回应只生成一次；源快照 SHA-256 未变化。

| 输入 | 实际回应表现 | 结论 |
|---|---|---|
| 质疑告示真实性 | 说明不知道发布衙门、不能辨认官印 | 回答对象正确，但补出“没见官差来管过”的无依据经历 |
| 询问刀客去向、落脚地 | 说明不知道方向和落脚地 | 未再整段讲镇子不太平，但仍补出“夜里收摊早”的原因 |
| 询问是否有人来茶摊打听告示 | 表示不知道 | 承接了问题 |
| 表示不生事、替老人留意刀客 | 老人感谢留意刀客的心意 | 协助对象保持正确，没有改成“相信告示” |

原投诉的背景整段复述在最新样本中未再出现；但未选择的“过路镖师、不生事、留意刀客”选项连续重复，旁白也反复采用同一暮色/茶碗铺垫。无依据经历来自规划 brief，表达器继续照写。这些是剩余问题，不把机械测试成功当作语义通过。中间样本 `tmp/planner-content-dialogue-7aa43e47-dccd-46e9-a9f7-4a9e5db3b61d` 同样保留，未用作成功证据。

后续应收敛规划器内容决策与上下文使用，尤其是让 brief 只包含本轮内容、避免把历史当事实、保证两个候选都推进对话；不新增模型审核或生成后文本改写来掩盖问题。

工程验证：完整 Vitest 236 文件通过、3103 项通过、4 项跳过；typecheck 与 127 项依赖边界检查通过；修改文件 ESLint 无错误（测试 harness 有一条既存 unused 参数提醒）；`git diff --check` 通过。历史查询有真实 SQLite 隔离测试，确认同游戏/NPC、已发布状态、旧 revision、最多六条，并只给 planning 注入、不写回存档。文档检查通过，系统文档长度有软阈值提醒。

## 本轮修复与对照

保留完整内容传递和单次 NPC 回应，不改数据结构、历史查询或审批。规划职责收口到系统消息，本轮资料放最后；三个润色器最后收到唯一内容稿。移除选项润色器自行决定问题是否合适的职责，用实际候选 ID 要求完整返回两项。固定选项的 label 明确显示为“玩家原话”，不再显示“无”；可选 atmosphere 不被当成必选任务，连续对白只承接当前话语。普通决策省略本轮无关的世界增量说明，保留原 PlanProposal 契约；专用格式模板实验未改善语义，已移除。

仅修提示仍未稳定解决语义。以下全部样本保留在 `tmp/planner-content-dialogue-<ID>/`，各有 `result.json` 和 `audit/events.jsonl`；不能因为接口成功就算通过。

| ID | provider 调用 | 已发布回应 | 失败发现 |
|---|---:|---:|---|
| 57141d29-53d8-4edc-85f4-6e52e6e1d081 | 8 | 1 | 重讲背景，后续规划缺表达单元 |
| 317bc216-cd62-452c-b2ab-2d4de53d6c8e | 16 | 4 | 背景及未选协助重复 |
| c019daba-a3d2-4aed-afb3-724a4816d650 | 16 | 4 | 规划补出早收摊等经历，继续重问 |
| 76e12212-f7f0-4dab-aba4-92e8ebd58fa5 | 15 | 2 | 未知回应变简洁，但选项润色连续只返回一项 |
| 49b527de-ee8c-4dbc-82c8-6259146432c8 | 16 | 4 | 编造玩家经历，重复未选问题 |
| d4837771-c8cb-499c-b2c8-be0b20c8e6ac | 16 | 4 | 专用模板减少环境复述，但问询引用丢失，补写营业情况 |
| b11a8701-b638-4c75-a8b6-7ec5d326942b | 18 | 4 | 回到旧背景，编造喝茶、歇脚等已发生情节 |
| bcbfb93a-2b60-44cc-a8a5-3fdf9a668330 | 13 | 2 | 可选 atmosphere 被填为裸字符串，规划校验失败 |
| 8644fc22-56bb-494c-936d-2d23f9ac109c | 16 | 4 | 接口成功，仍补早收摊/脚步声并重复选项 |
| c047e467-13f1-49b6-bfa8-f8779fc2259c | 16 | 4 | 恢复原契约后仍重讲 fact_1，证明只改提示尚不够 |

随后做相同模型、相同存档、相同提示的限定对照，仅在测试进程把已有 `AI_RUNTIME_THINKING_ROLES` 设为 `planning`。样本 `ea21320e-15a9-4f88-86ff-bfaab3c57a53` 共 17 次调用：planning 5 次（含一次重试），其余三个角色各 4 次；约 148 秒。NPC 从承认告示真假不明，转为回应帮忙递消息、接受避让条件，最后直接承认不知道刀客落脚处。未再整段重讲背景，协助没有变成相信告示；没有新审核角色。

据此仅将 RPG planning 的既有 thinking 默认改为 on，其他角色、模型、预算和重试不变。生产环境未配置该变量时生效；显式列表仍覆盖默认，显式空值仍关闭全部。`.env.local` 未修改。这个对照说明规划思考在本样本中改善内容取舍，不证明模型永不犯错。

默认策略复验 `b30354a6-cd45-4998-907b-813c74863512`：四次回应、18 次调用，planning 6 次（含两次重试），三个表达角色各 4 次，约 173 秒。整段背景重复消失，但仍将资料缺失的消息来源写成“分不清亲眼还是听说”，隔轮候选也重新使用了不牵连的态度。因此最后再明确：历史每轮两项候选都须比较；连续未知后不换相邻维度盘问；缺知识来源不等于记不清自身经历。

最终样本与工程结果见下一节。

## 最终样本验收

`tmp/planner-content-dialogue-21603a1a-b7b7-4632-a9b3-620ef40e0529` 使用实际默认配置，`plannerThinkingOverride=false`；审计确认 planning=on，narration/character/choices=off。原问题重放加三次连续选择均发布成功，源快照 SHA-256 未变化。

| 本次输入 | NPC 内容及衔接 | 结果 |
|---|---|---|
| 质疑告示真实性 | 直接说明不知道真假及官印来历 | 没有再补整段镇子不太平的背景 |
| 说明过路歇脚、不惹麻烦 | 承接玩家表态，保持老人谨慎的态度 | 没有把态度改成相信告示或替玩家完成行动 |
| 提出双方互不打听 | 老人接受，提出摊前不出鞘、不引来麻烦的条件 | 条件来自当前回应和角色已有目标，没有补写往日经历 |
| 请老人说清规矩、表示愿意照办 | 一次讲清三条规矩及守规矩便不再多问的回应 | 多段仍是一次 character 生成，完整承接本次要求 |

新候选依次围绕来意/协助、质疑试探/互不打听、询问规矩/质疑回避、拒绝规矩/寻找住宿展开，没有把前轮整条未选候选重新保留。旧承诺和当下规矩可以在回应中承接，不把必要承接当作背景复述。旁白只承接当前玩家原话，未反复铺设暮色、炉火或茶碗。

本样本通过本次问题的语义验收；与思考对照样本共同覆盖未知问答、协助对象和态度/条件承接。没有验证待确认 UI 问题，也不把有限样本当成所有未来对白的质量保证。

实际 17 次 provider 调用：planning 5 次，narration、character、choices 各 4 次。每次角色回应对应一个完整 character 单元；四轮均先规划，再按依赖执行三个表达角色。额外一次 planning 是现有 6000-token 上限触发输出截断后的既有重试，并非新增审核或拆段调用。

**性能代价：四次回应总计约 292 秒，平均约 73 秒；其中包含一次规划截断重试。** 思考对照约 148 秒，前一默认样本约 173 秒，关闭思考的最后对照约 42 秒。默认规划思考改善了本组语义，但增加了等待时间，且既有输出预算仍可能触发重试；本轮没有扩大预算、改模型或添加其他调用环节。

工程验证：完整 Vitest 236 文件、3107 项通过，4 项显式 live 测试跳过；最后两句提示调整另跑 112 项相关测试通过。typecheck、127 项依赖边界、修改文件 ESLint、`git diff --check` 均通过。文档检查 0 错误，运行时文档长度仍有软阈值提醒。未提交或合并，HEAD 仍为指定基线。
