# P3 四项规则与玩法收尾

## 范围

从 `af70f83a` 继续 [P3 Plan](../plans/2026-09-15-narrative-architecture-p3.md) 的四项收尾：B 披露后果、完整故事实际回访、另一题材复用、同源双路线 live/UI。不调整文学质量，不修改 main 或阶段指针。历史失败批次保持原判定。

## 实施证据

### 匠人材料复用

`src/game/application/testing/narrativeP3CraftJourney.test.ts` 构造独立陶工作坊初态，通过正式后果绑定审批、互动提案安装和 `resolveTurn` 验证：私下筛检泥样→告知助手，窑师保持不知情→实际告知窑师，目标完成→指导试烧成为可执行方法→实际试烧产生唯一发现。未开放方法不可提前执行，重复操作不重复产生目标或发现；没有物品递送、信筒或原故事人名依赖。

该片段发现并修复了空目标条件契约不一致：Plan 允许空 completeWhen/blockWhen 不触发对应状态，审批和 Entity 原先拒绝。两处校验已对齐；显式空调查 requirements 同样按无前提处理，任务完成与合作的非空约束保持。相关 7 个文件共 145 项测试通过，类型检查通过；独立复核的规范与质量两项通过。这是正式规则复用证据，完整 SQLite/UI 故事另行验证。

### B 披露后果

`f12daf22` 将当前场景的实际 NPC 听众、知识事件及目标/Quest 后果接入同候选编译、审阅与最终单次 CAS。未来场景不授知，失败候选不落知识和目标；幕末披露必须提供推进后所需的合法内容。普通目标、幕末目标、失败后重试和并发发布均有正式 SQLite 回归。

独立复核发现同场转述绕过传播权限、目标完成后重复绑定误拒两处问题，`3687dde1` 改为逐句重建面向实际听众的权限，以及允许已安装的相同绑定幂等重验。全量检查还发现当前表达式预检误用终局的零选项约束，导致 P2 故事回归；已修复并保留真实选项后续校验。限定 280 项测试和未改写的 P2 完整故事 5 项通过；最终限定复核的规范与质量均通过，独立重跑原两条复现均已转绿。

### 完整故事回访

用户已确认允许“玩家在外取得、被旧 NPC 已批准任务/合作条件明确引用的新证据”触发受控回访。`44d66ec2` 接通规则和完整双路故事，`5dc0dcd9` 修正回访当前 NPC 别名不能继承未来返程 NPC。关联仍要求实际可见来源、已访问地点与最后展示事件水位，不自动给旧 NPC 授知。

私下故事实际事件顺序为离场、调查、回访、告知、目标变化、核验、返程、交付。告知前同时核对 Entity 知识与 NPC 观察者投影，均不知情；告知后的 player_told 来源精确引用真实分享事件。返程经 raw `objective_return` 编译、解析、审批与 CAS，只准备已访问相邻的未完成交谈目标，实际移动仍由玩家选择。公开路线保持不同调查见证与合作准入，两路均正常终局且交付一次，调查后重载记录一致。

### 有界 live/UI

新增薄 UI 执行入口 `npm run journey:narrative:p3:ui`，复用现有 Next 页面和生产 entry，私下路线通过实际浏览器提交 driver 当步选中的正式 token/自由策略，公开路线从同一初态通过 API 完成。原 driver 保留 Action 身份、调用预算、绝对截止时间和 tape，不增加第三条路线。执行器选择在初始化前以协议/代码指纹登记，禁止在 UI 重新创建、清档或手动续预算。

修正私下策略必须早于调查、重载必须在调查之后、完成判定须对应真实证据的分享/目标/核验与受控回访。UI 中途刷新比较正式状态哈希，终局刷新不能替代中途刷新。31 项相关 Node 测试通过；独立复核提出的启动清理、终局刷新误判、关闭异常跳过结算三项已修复并通过限定复核。

本轮在 `ecfe557b` 零网络登记 `p3-closeout-01`。初次执行曾被自动审批要求确认具体发送目标与范围，未发出请求；用户明确授权配置的 API 地址后，用原冻结协议执行。共同前缀完成 2 次真实行动，换幕候选在有限审阅循环后失败：共同 HTTP 12，两路均为 `blocked/P3_SHARED_PREFIX_FAILED`，路线动作均为 0，尚未打开 UI。计数没有重复计算共同前缀。独立目录零 HTTP 严格回放复现同样状态和失败码，`strictReplayPassed=true`，玩法 `passed=false`。

真实请求确认一处程序契约矛盾：同一候选的作者请求要求创建下一幕、当前 objectiveLink 为 null；审阅请求却基于预先具象化的结果状态重新计算，要求 worldDelta 为 null、将刚创建实体视为既存实体，并要求绑定新 Quest。连续候选重复该矛盾，属于状态边界错误。独立诊断保留在 `artifacts/narrative-p3-closeout/live-01-review-diagnosis.md`；这不撤销审阅中独立的未授权事实问题。

`6ff87097` 将审阅的生成契约保留在合法本场披露后、worldDelta 物化前，候选实体、权限及执行依据继续使用完整预览。正式请求回归覆盖普通换幕、幕末披露与结局准备，推进职责读取同一生成契约。返程图同时补齐 step basis 与实际抵达快照，当前场景位置不提前变化。独立限定复核通过，报告为 `artifacts/narrative-p3-closeout/contract-final-review.md`。新批次从这些具体程序修复后重新冻结，不以改稿或重抽开局替代修复。

`863ea9cb` 冻结的 `p3-closeout-02` 共用前缀完成 4 次行动，成功换幕，生成/审阅契约矛盾未重现；12 次真实 HTTP 后两路均因 `P3_CAPABILITY_COVERAGE_FAILED` 阻断，路线动作仍为 0，UI 未启动。零 HTTP 严格回放复现相同结果。实际请求保留了完整调查、回访与合作设定，但作者严格字段清单遗漏 consequenceBindings，缺少目标/合作绑定 schema，调查说明与输出骨架也不一致。模型输出 newFact:null 不符合本次设定；同时程序确有能力契约遗漏，不能将失败仅归为模型发挥。独立证据见 `artifacts/narrative-p3-closeout/live-02-capability-diagnosis.md`。

`21ed29fe` 补齐现有四类绑定和五类条件的作者契约；已选 NPC 仅提供 goalOrdinal/goalId/hasResolution 引用元数据，不扩大实体闭包或公开私密目标。示例经正式 parser 和绑定审批验证，220 项相关测试、类型与限定 lint 通过。最终复核进一步对齐绑定只能用当前 NPC 正式 ID、主动调查要求非空标签、已有合法未发现 scene fact 可首次绑定三处说明，66 项限定测试通过；新增正式审批正反例与实际发送请求断言。复核无遗留重要问题，最终 production build 通过，详见 `artifacts/narrative-p3-closeout/capability-final-review.md`。普通故事仍可省略绑定；没有新增操作或强制生成固定剧情。

## 最终检查与剩余边界

已实现切片的最终集成审阅通过，无未解决的重要跨模块发现。生成契约修复后全量为 247 个文件通过、1 个跳过，3102 项测试通过、1 项跳过。类型检查、改动文件 lint、production build 通过；文档检查 0 错误、1 个既有篇幅提醒，diff 检查通过。本轮 P1/P2/P3 的 66 项 Node 脚本测试全部通过；此前完整 lint 和 135 项依赖边界检查通过。首次失败与修复后日志均保留在 `artifacts/narrative-p3-closeout/`，不以旧批次成绩替代新验证。

提交包括 B 主链 `f12daf22`、匠人复用 `373a4326`、B 复核修复 `3687dde1`、有界 UI 入口 `526e3688`，本轮回访选路 `01e07cf7`、完整回访 `44d66ec2`、在场别名修正 `5dc0dcd9` 与生产 raw 返程链 `f64176d9`。未修改 main、阶段指针或历史真实批次判定。

四项中，B 披露后果、另一题材最小复用与同故事实际回访已完成；live/UI 尚需真实批次证据，不能将本报告视为 P3 全部完成。
